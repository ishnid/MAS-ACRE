package is.lill.acre.event;

import is.lill.acre.protocol.Protocol;

public class UnknownProtocolEvent extends AbstractProtocolEvent {

    public UnknownProtocolEvent( Protocol p ) {
        super( p );
    }
}
